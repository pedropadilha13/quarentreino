<h1>Página de aluno</h1>
Olá, <%= locals.user.nome %>! <% locals.treinos.forEach(treino => { %>
<span><%= treino.nome %> </span>
<% locals.treino.conteudo.forEach(exercicio => { %>
<span><%= treino.conteudo  %> </span>
<% }); %>
